[project]
name = "wet-run-monomer-folding"
version = "1.0.0"
description = "Terminal-only budgeted ESMFold monomer prediction on Modal"
requires-python = ">=3.10,<3.14"
dependencies = [
    "modal==1.5.3",
]

[tool.uv]
package = false
